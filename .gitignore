# deps / build
node_modules/
dist/
*.vsix

# secrets — never commit (see Audit&Fix secret hygiene)
.env
.env.*
.dev.vars
*.local

# cloudflare / wrangler
.wrangler/
.mf/

# os / editor
.DS_Store
*.log

# MinSpec ephemeral data
.minspec/session.json
.minspec/calibration.json

.minspec/generated-hashes.json
.minspec/template-baseline.json
# Machine-local UI dismissal state (answeredSignatures) + harness worktree
# checkouts — rewritten/created at runtime, never committed (G-8, minspec parity).
.minspec/preferences.json
.claude/worktrees/

.minspec/queue/

.minspec/sessions/
